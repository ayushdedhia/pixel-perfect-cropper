[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# Handle SPA routing - redirect all non-API, non-file requests to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
